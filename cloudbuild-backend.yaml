steps:  
  - name: 'gcr.io/cloud-builders/docker' 
  args: ['build', '-t', 'gcr.io/$PROJECT_ID/app-backend', './backend'] 
  
  - name: 'gcr.io/cloud-builders/docker' 
  args: ['push', 'gcr.io/$PROJECT_ID/app-backend'] 
  
  - name: 'gcr.io/cloud-builders/gcloud' 
  args: [ 
    'run', 'deploy', 'app-backend', 
    '--image', 'gcr.io/$PROJECT_ID/app-backend', 
    '--platform', 'managed', 
    '--region', 'us-central1', 
    '--allow-unauthenticated', 
    '--port', '5000' 
  ] 

options: 
  logging: CLOUD_LOGGING_ONLY
